echo "# AI Phishing Website Detector

A Flask app that uses machine learning to detect phishing websites based on URL features.

## How to Run

1. Install dependencies
2. Run `app.py`
3. Visit `http://localhost:5000`

" > README.md
